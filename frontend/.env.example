# Base URL of the api-gateway. Leave empty to use the dev server proxy / same-origin nginx.
VITE_API_BASE_URL=

# Stripe publishable key. The checkout page also reads it from /api/v1/payments/methods,
# so this is only needed if you want it baked into the bundle.
VITE_STRIPE_PUBLISHABLE_KEY=
