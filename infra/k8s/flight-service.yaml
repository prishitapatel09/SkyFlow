apiVersion: apps/v1
kind: Deployment
metadata:
  name: flight-service
  namespace: skyflow
  labels:
    app: flight-service
    app.kubernetes.io/part-of: skyflow
spec:
  replicas: 3
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: flight-service
  template:
    metadata:
      labels:
        app: flight-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      # Spring Boot needs time to drain in-flight requests; the app is configured for graceful
      # shutdown, so give it room before SIGKILL.
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: flight-service
          image: ${ECR_REGISTRY}/skyflow-flight-service:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8081
          env:
            - name: PORT
              value: "8081"
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef: { name: skyflow-config, key: LOG_LEVEL }
            - name: DB_URL
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: db-url }
            - name: DB_USER
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: db-user }
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: db-password }
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: redis-host }
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: redis-password }
            - name: RABBITMQ_HOST
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: rabbitmq-host }
            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: rabbitmq-user }
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef: { name: skyflow-secrets, key: rabbitmq-password }
          resources:
            requests:
              cpu: "400m"
              memory: "768Mi"
            limits:
              memory: "1Gi"
          startupProbe:
            httpGet: { path: /actuator/health/liveness, port: http }
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: http }
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: http }
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      imagePullSecrets:
        - name: ecr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: flight-service
  namespace: skyflow
  labels:
    app: flight-service
spec:
  type: ClusterIP
  selector:
    app: flight-service
  ports:
    - name: http
      port: 8081
      targetPort: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: flight-service
  namespace: skyflow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: flight-service
  minReplicas: 3
  maxReplicas: 12
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: flight-service
  namespace: skyflow
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: flight-service
